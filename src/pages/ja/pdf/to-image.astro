---
import ToolLayout from '@layouts/ToolLayout.astro';
import PdfToImage from '@components/pdf/PdfToImage';
import type { Locale } from '../../../i18n/locales';
import { t } from '../../../i18n/utils';

const lang: Locale = 'ja';
const tr = t(lang);
const toolTr = tr.tools.pdfToImage;
const title = `${toolTr.name} - ${tr.site.name}`;
const description = toolTr.description;
const uiStrings = { common: tr.common, pdfToImage: tr.pdfToImage };
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebApplication',
  name: toolTr.name,
  description: toolTr.description,
  url: 'https://keepdf.com/ja/pdf/to-image',
  applicationCategory: 'UtilityApplication',
  operatingSystem: 'Any',
  inLanguage: lang,
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
};
---

<ToolLayout
  title={title}
  toolName={toolTr.name}
  description={description}
  category="pdf"
  jsonLd={jsonLd}
  lang={lang}
>
  <PdfToImage client:only="react" translations={uiStrings} />
</ToolLayout>
