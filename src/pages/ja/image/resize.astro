---
import ToolLayout from '@layouts/ToolLayout.astro';
import ResizeImage from '@components/image/ResizeImage';
import type { Locale } from '../../../i18n/locales';
import { t } from '../../../i18n/utils';

const lang: Locale = 'ja';
const tr = t(lang);
const toolTr = tr.tools.imageResize;
const title = `${toolTr.name} - ${tr.site.name}`;
const description = toolTr.description;
const uiStrings = { common: tr.common, imageResize: tr.imageResize };
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebApplication',
  name: toolTr.name,
  description: toolTr.description,
  url: 'https://keepdf.com/ja/image/resize',
  applicationCategory: 'UtilityApplication',
  operatingSystem: 'Any',
  inLanguage: lang,
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
};
---

<ToolLayout
  title={title}
  toolName={toolTr.name}
  description={description}
  category="image"
  jsonLd={jsonLd}
  lang={lang}
>
  <ResizeImage client:only="react" translations={uiStrings} />
</ToolLayout>
