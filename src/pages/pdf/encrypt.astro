---
import ToolLayout from '@layouts/ToolLayout.astro';
import EncryptPdf from '@components/pdf/EncryptPdf';
import type { Locale } from '../../i18n/locales';
import { t } from '../../i18n/utils';

const lang: Locale = 'en';
const tr = t(lang);
const toolTr = tr.tools.pdfEncrypt;
const title = `${toolTr.name} - ${tr.site.name}`;
const description = toolTr.description;
const uiStrings = { common: tr.common, pdfEncrypt: tr.pdfEncrypt };
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebApplication',
  name: toolTr.name,
  description: toolTr.description,
  applicationCategory: 'UtilityApplication',
  operatingSystem: 'Any',
  inLanguage: lang,
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
};
---

<ToolLayout
  title={title}
  toolName={toolTr.name}
  description={description}
  category="pdf"
  jsonLd={jsonLd}
  lang={lang}
>
  <EncryptPdf client:only="react" translations={uiStrings} />
</ToolLayout>
