---
import ToolLayout from '@layouts/ToolLayout.astro';
import PdfToWord from '@components/pdf/PdfToWord';
import type { Locale } from '../../i18n/locales';
import { t } from '../../i18n/utils';

const lang: Locale = 'en';
const tr = t(lang);
const toolTr = tr.tools.pdfToWord;
const title = `${toolTr.name} - ${tr.site.name}`;
const description = toolTr.description;
const uiStrings = { common: tr.common, pdfToWord: tr.pdfToWord };
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebApplication',
  name: toolTr.name,
  description: toolTr.description,
  url: 'https://keepdf.com/pdf/to-word',
  applicationCategory: 'UtilityApplication',
  operatingSystem: 'Any',
  inLanguage: lang,
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
};
---

<ToolLayout
  title={title}
  toolName={toolTr.name}
  description={description}
  category="pdf"
  jsonLd={jsonLd}
  lang={lang}
>
  <PdfToWord client:only="react" translations={uiStrings} />
</ToolLayout>
