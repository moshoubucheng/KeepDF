---
import ToolLayout from '@layouts/ToolLayout.astro';
import ConvertImage from '@components/image/ConvertImage';
import type { Locale } from '../../i18n/locales';
import { t } from '../../i18n/utils';

const lang: Locale = 'en';
const tr = t(lang);
const toolTr = tr.tools.imageConvert;
const title = `${toolTr.name} - ${tr.site.name}`;
const description = toolTr.description;
const uiStrings = { common: tr.common, imageConvert: tr.imageConvert };
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebApplication',
  name: toolTr.name,
  description: toolTr.description,
  url: 'https://keepdf.com/image/convert',
  applicationCategory: 'UtilityApplication',
  operatingSystem: 'Any',
  inLanguage: lang,
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
};
---

<ToolLayout
  title={title}
  toolName={toolTr.name}
  description={description}
  category="image"
  jsonLd={jsonLd}
  lang={lang}
>
  <ConvertImage client:only="react" translations={uiStrings} />
</ToolLayout>
